name: Grain6pbc Repository Sync

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  sync-grain06pbc-repos:
    runs-on: ubuntu-latest
    if: github.repository == 'grain06pbc/grain6'
    
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Sync to grain06pbc/grainkae3g
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Clone target repository
          git clone https://github.com/grain06pbc/grainkae3g.git target-repo
          cd target-repo
          
          # Copy all files from source
          rsync -av --exclude='.git' ../. .
          
          # Commit changes
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          
          if git diff --quiet; then
            echo "No changes to sync"
          else
            git commit -m "Sync from grain06pbc/grain6: ${{ github.sha }}"
            git push origin main
            echo "✅ Synced to grain06pbc/grainkae3g"
          fi
      
      - name: Sync to grain06pbc/graincontacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Clone target repository
          git clone https://github.com/grain06pbc/graincontacts.git target-repo
          cd target-repo
          
          # Copy all files from source
          rsync -av --exclude='.git' ../. .
          
          # Commit changes
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          
          if git diff --quiet; then
            echo "No changes to sync"
          else
            git commit -m "Sync from grain06pbc/grain6: ${{ github.sha }}"
            git push origin main
            echo "✅ Synced to grain06pbc/graincontacts"
          fi
      
      - name: Sync to grain06pbc/humble-stack
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Clone target repository
          git clone https://github.com/grain06pbc/humble-stack.git target-repo
          cd target-repo
          
          # Copy all files from source
          rsync -av --exclude='.git' ../. .
          
          # Commit changes
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          
          if git diff --quiet; then
            echo "No changes to sync"
          else
            git commit -m "Sync from grain06pbc/grain6: ${{ github.sha }}"
            git push origin main
            echo "✅ Synced to grain06pbc/humble-stack"
          fi
      
      - name: Sync to grain06pbc/graindaemon
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Clone target repository
          git clone https://github.com/grain06pbc/graindaemon.git target-repo
          cd target-repo
          
          # Copy all files from source
          rsync -av --exclude='.git' ../. .
          
          # Commit changes
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          
          if git diff --quiet; then
            echo "No changes to sync"
          else
            git commit -m "Sync from grain06pbc/grain6: ${{ github.sha }}"
            git push origin main
            echo "✅ Synced to grain06pbc/graindaemon"
          fi
      
      - name: Update repository descriptions
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Update grain06pbc/grainkae3g description
          gh api repos/grain06pbc/grainkae3g --method PATCH \
            --field description="🌾 Grain6pbc Personal: grainkae3g | Live Site: https://kae3g.grain06pbc.com/ | Synced from grain06pbc/grain6"
          
          # Update grain06pbc/graincontacts description
          gh api repos/grain06pbc/graincontacts --method PATCH \
            --field description="🌾 Grain6pbc Identity: graincontacts | Live Site: https://contacts.grain06pbc.com/ | Synced from grain06pbc/grain6"
          
          # Update grain06pbc/humble-stack description
          gh api repos/grain06pbc/humble-stack --method PATCH \
            --field description="🌾 Grain6pbc UI: humble-stack | Live Site: https://ui.grain06pbc.com/ | Synced from grain06pbc/grain6"
          
          # Update grain06pbc/graindaemon description
          gh api repos/grain06pbc/graindaemon --method PATCH \
            --field description="🌾 Grain6pbc Automation: graindaemon | Live Site: https://daemon.grain06pbc.com/ | Synced from grain06pbc/grain6"
          
          echo "✅ Updated all repository descriptions"
