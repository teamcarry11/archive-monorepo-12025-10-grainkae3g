{:paths ["src"]
 :deps {}
 
 :tasks
 {:requires ([babashka.process :refer [shell]])
  
  ;; Build task for CI/CD - delegates to qb-now
  qb-now
  {:doc "Quick build - generate grainsite content (delegates to grain6pbc/qb-now)"
   :task (do
           (println "🌾 grainbarrel (team04): Delegating to qb-now...")
           (shell "bb" "../qb-now/scripts/qb-now.bb"))}
  
  ;; GB aliases (grainbarrel = team04, qb → gb migration)
  gb-now
  {:doc "GB NOW - Same as qb-now (team04 grainbarrel)"
   :task (shell "bb" "../qb-now/scripts/qb-now.bb")}
  
  gb-fast
  {:doc "GB FAST - Fast incremental build"
   :task (do
           (println "🌾 gb-fast: Fast incremental build")
           (shell "bb" "../qb-now/scripts/qb-now.bb" "--fast"))}
  
  flow
  {:doc "Complete build + deploy flow (delegates to teambright01/grainbarrel)"
   :task (shell {:dir "../../teambright01/grainbarrel"} "bb" "flow" (or (first *command-line-args*) "chore: grainbarrel team04 flow"))}}}

